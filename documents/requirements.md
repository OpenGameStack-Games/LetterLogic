# LetterLogic Requirements

## 1. Overview
LetterLogic is a word-guessing game inspired by Wordle, built using the Godot Engine. The unique twist in LetterLogic is that all valid words and secret target words are **5-letter isograms** (words containing no repeating letters).

## 2. Core Gameplay
- **REQ-2.1 - Grid Size:** The game board consists of a 6-row by 5-column grid. Players have a maximum of 6 attempts to guess the 5-letter secret word.
- **REQ-2.2 - Tile Evaluation & Staggered Reveal Animation:** After a valid guess is submitted, each letter tile is evaluated and colored based on its presence in the secret word:
  - **Correct (Green / 🟩):** The letter is in the secret word and in the correct position.
  - **Present (Yellow / 🟨):** The letter is in the secret word but in the wrong position.
  - **Absent (Flat Red / 🟥):** The letter is not in the secret word.
  - **Staggered Reveal & Scale Pop:** Tile states and colors are revealed sequentially from left to right across the row with a staggered delay (0.3 seconds per column, `col * 0.3`). Upon each tile's reveal, it performs a 0.3-second scale 'pop' animation (scaling up to 1.1x with `TRANS_SINE` / `EASE_OUT` over 0.15s, then settling back to 1.0x with `TRANS_SINE` / `EASE_IN` over 0.15s) centered around its midpoint pivot (`pivot_offset = size / 2.0`) without disrupting parent grid layout calculations. The reveal animation applies uniformly across Continuous Play and Daily Challenge modes.
- **REQ-2.3 - Win/Loss Condition:** The game is won if the player guesses the secret word exactly within 6 attempts. The game is lost if the 6th attempt is incorrect.
- **REQ-2.4 - Keyboard State Update:** The on-screen virtual keyboard must update its keys to reflect the best-known state of each letter (Correct > Present > Absent) based on all submitted guesses. Absent keys display in flat red (`#b53b3b`) with white text (`#ffffff`).
- **REQ-2.5 - Active Gameplay Timer:** An active running timer is displayed in the game header directly above the grid during gameplay. The timer starts at `00:00` upon puzzle start, increments dynamically while solving, and stops immediately upon game completion (win or loss). Time is formatted as `MM:SS` (or `HH:MM:SS` if the puzzle duration exceeds 1 hour).
- **REQ-2.6 - App Lifecycle & Overlay Timer Pausing:** The active gameplay timer must automatically pause when the application loses focus, is minimized, or is sent to the background (`NOTIFICATION_APPLICATION_FOCUS_OUT`, `NOTIFICATION_APPLICATION_PAUSED`), and automatically resume when primary focus returns (`NOTIFICATION_APPLICATION_FOCUS_IN`, `NOTIFICATION_APPLICATION_RESUMED`). Furthermore, modal dialogs and full-screen overlays (such as the Statistics Screen or Game Over modal) must pause the timer while visible, resuming only once dismissed if the game remains in progress.
- **REQ-2.7 - Game Over Modal & Win Summary Display:** Upon puzzle completion (win or loss), the game waits for the staggered tile reveal animation across all 5 columns to complete (~1.5 seconds) before displaying the Game Over modal. When displayed, the modal plays an entrance animation (scaling from 0.8x to 1.0x with `TRANS_BACK` / `EASE_OUT` and fading from 0.0 to 1.0 opacity over 0.3s) centered at its midpoint pivot (`pivot_offset = size / 2.0`). This entrance animation applies uniformly across both win and loss states:
  - **Win Screen:** Displays an achievement title based on attempts (e.g., "Genius!", "Magnificent!", "Impressive!", "Splendid!", "Great!", "Phew!") and a two-line summary message:
    - Line 1: `You found '<secret>' in <attempts>/6 guesses.`
    - Line 2: `Time: MM:SS` (or `Time: HH:MM:SS` if the elapsed solve time reaches or exceeds 1 hour), displaying the final frozen active play time retrieved from `GameManager.get_active_time()` and formatted via `GameManager.format_time()`. If `GameManager` is unavailable or returns 0, it gracefully falls back to `Time: 00:00`.
  - **Loss Screen:** Displays title "Game Over" and the single-line message `The word was <secret>`. The elapsed solve time is strictly omitted on loss screens.
  - **Typography Scaling:**
    - Modal Title (`TitleLabel`): Font size enlarged to 56px (`theme_override_font_sizes/font_size = 56`), horizontally centered.
    - Summary Message (`MessageLabel`): Font size enlarged to 32px (`theme_override_font_sizes/font_size = 32`), horizontally centered (`horizontal_alignment = 1`) with autowrap enabled (`autowrap_mode = 2`), fitting cleanly without clipping or overflow.
    - Action Buttons (`NextWordButton`, `ShareButton`, `MenuButton`): Font size enlarged to 32px (`theme_override_font_sizes/font_size = 32`).
  - **Button Sizing & Inset Layout:**
    - Action buttons feature a doubled minimum height of 96px (`custom_minimum_size = Vector2(0, 96)`), doubled from 44-48px, providing comfortable touch targets.
    - Action buttons are wrapped in a `MarginContainer` (`ButtonMargin`) with 64px horizontal margins (`margin_left = 64`, `margin_right = 64`), insetting them from the modal panel edges by ~20% so they present as centered button controls rather than edge-to-edge bars.
    - Vertical separation between buttons is set to 12px (`theme_override_constants/separation = 12`).
    - Buttons retain standard 1930s monochrome styling (dark charcoal fill, 2px solid white border, 8px rounded corners, crisp white text) and dynamic visibility ("Next Word" in Continuous Play, "Share Results" in Daily Challenge, "Main Menu" in both).

## 3. Input & Validation
- **REQ-3.1 - Isogram Typing Constraint:** While typing a guess, the game must prevent the user from inputting a letter that already exists in the current active row. Keys currently typed in the active row are temporarily disabled and visually styled in dark gray (`#272729`), maintaining clear differentiation from absent letters (flat red / `#b53b3b`).

- **REQ-3.2 - Dictionary Validation:** The game must reject guesses that are not present in the internal dictionary of 5-letter isograms. A rejected guess does not consume an attempt.
- **REQ-3.3 - Length Validation:** The game must reject guesses that are shorter than 5 letters.
- **REQ-3.4 - Duplicate Word Submission Prevention:** The game must prevent players from submitting the same word multiple times within the same puzzle across all game modes (Continuous Play and Daily Challenge). If a submitted valid word has already been guessed in the current puzzle, the submission is rejected without consuming an attempt or advancing the active row, and a toast message stating "Word already guessed" is displayed.

## 4. Game Modes
- **REQ-4.1 - Continuous Play:** A sandbox mode where players can play unlimited consecutive games. Secret words are selected randomly from the word bank. The game screen header displays `CONTINUOUS PLAY`.
- **REQ-4.2 - Daily Challenge:** A synchronized daily mode where all players attempt to guess the same deterministic secret word, based on the current UTC date. The game screen header displays `DAILY CHALLENGE • YYYY-MM-DD` reflecting the current UTC date. At UTC rollover (midnight UTC), a new daily puzzle begins; any uncompleted in-progress Daily Challenge session from a previous UTC day is discarded and cleared, starting a fresh board with the new day's secret word.
- **REQ-4.3 - Daily Lockout & Completed Summary Routing:** A player can only play and complete the Daily Challenge once per UTC day. When the daily puzzle is completed (won or lost), the Main Menu navigation button updates its primary label and countdown timer:
  `Daily Challenge - Completed\n[Next in: HH:MM:SS]`
  The countdown updates every second until UTC midnight reset. Pressing the completed button does not reset the board, pick a new word, or trigger new statistics entries; instead, it loads the saved daily game session and opens `res://scenes/main_game.tscn` directly into the Game Over summary state. The GameBoard and virtual keyboard are repopulated with the completed guesses and evaluated key states, the header timer displays the saved solve time and remains frozen, the `GameOverModal` is presented immediately with the "Share Results" button available and "Next Word" hidden, and in-game input (typing and virtual keyboard) is disabled.

## 5. Statistics Tracking
- **REQ-5.1 - Segregated Stats:** The game must track statistics separately for "Continuous Play" and "Daily Challenge" modes.
- **REQ-5.2 - Tracked Metrics & Whole Number Formatting:** The game must record total games played, total games won, current win streak, maximum win streak, best solve time (fastest win), average solve time, and a distribution of guess attempts (1 through 6, and losses). Plays (`played`), Current Streak (`current_streak`), and Max Streak (`max_streak`) values must be rendered strictly as whole integer numbers without floating-point decimal points across all game modes and tabs on the Statistics Screen. Deserialized counts from persistent storage must enforce integer type conversion so that floating-point numbers do not propagate into runtime state or UI labels.
- **REQ-5.3 - Average and Best Solve Time Rules:** Best and Average Solve Times must be tracked and displayed separately per mode on the statistics screen. Average solve time is strictly calculated across won games (losses are excluded from solve time calculations). If no games have been won in a mode, best and average times are displayed as `--:--`.
- **REQ-5.4 - Mode Selector Tabular Navigation & Multi-Row Summary Layout:** The mode selector on the Statistics Screen must use a connected tabular layout (`TabContainer`) presenting "Continuous Play" and "Daily Challenge" tabs. Switching tabs immediately updates all summary metrics and the guess distribution without reload delay or UI state corruption. Summary metrics are arranged in a 3-column multi-row grid (`GridContainer`, 2 rows of 3 cards) structured with intuitive vertical column pairings:
  - **Top Row (Overall Milestones & Peak Achievements):** Played (`PlayedCard`), Max Streak (`MaxStreakCard`), and Best Time (`BestTimeCard`).
  - **Bottom Row (Rates, Active Status & Averages):** Win Percentage (`WinPctCard`), Current Streak (`StreakCard`), and Average Time (`AvgTimeCard`).
  - **Vertical Column Pairings:** Column 1 pairs volume with success rate (Played / Win %), Column 2 pairs peak with active streak performance (Max Streak / Current Streak), and Column 3 pairs fastest with typical solve duration (Best Time / Average Time). This layout maximizes legibility and accommodates enlarged typography without horizontal crowding across both "Continuous Play" and "Daily Challenge" tabs.

## 6. Sharing
- **REQ-6.1 - Result Generation:** Upon completing a Daily Challenge, the game must generate a shareable text block containing the header (game title, UTC date, score/attempts), a dedicated timer line with the stopwatch emoji (`⏱️ MM:SS` or `⏱️ HH:MM:SS`), an emoji grid representing the game board, and a link to the game on the Google Play Store.
- **REQ-6.2 - Clipboard/Native Share:** The generated text must be copied to the system clipboard and, on Android, trigger the native system share sheet via the Godot Android Share plugin (`SharePlugin`). On non-Android platforms (or if the plugin singleton is absent), the game gracefully copies to clipboard without crashing.

## 7. Saving and Data Persistence
- **REQ-7.1 - Persistence & State Isolation:** The game must save player statistics and daily challenge records locally to the device so they persist between sessions. Upon puzzle completion (win or loss) in Daily Challenge mode, the completed game state—including `active_play_time`, submitted guesses, guess results, secret word, status, and keyboard states—is serialized and persisted to `user://save_daily.json` via `SaveManager`, and completion status is persisted to `user://daily_records.json` via `DailyManager`. Re-opening a completed Daily Challenge from the Main Menu restores this saved state without re-invoking `StatsManager.record_game()` or mutating player statistics.
- **REQ-7.2 - In-Progress Game State Persistence:** The active puzzle session must be automatically serialized and saved locally to `user://save_continuous.json` (Continuous Play) or `user://save_daily.json` (Daily Challenge) via `SaveManager` whenever an in-progress game loses application focus, is paused, or is sent to the background (`NOTIFICATION_APPLICATION_FOCUS_OUT`, `NOTIFICATION_APPLICATION_PAUSED`), receives a window close request (`NOTIFICATION_WM_CLOSE_REQUEST`), or when the player navigates back to the Main Menu via the header Back button (`BackButton`). The serialized session state strictly preserves the secret word, game mode, game status (`IN_PROGRESS`), current row, current guess, submitted guesses, evaluation result grids, keyboard letter states, and accumulated active play time.
- **REQ-7.3 - In-Progress Session Restoration & Reset Prevention:** Once a puzzle has started in either Continuous Play or Daily Challenge mode, the player must finish it (either guess the secret word or exhaust all 6 attempts) before the puzzle can reset. Navigating back into the mode from the Main Menu automatically deserializes the saved in-progress state into `GameManager` and restores the board and keyboard: the 6x5 grid repopulates with previously submitted guesses, evaluated tile states (Green, Yellow, Red), and any typed in-progress letters, the virtual keyboard restores evaluated key states and disables letters typed in the current row, the active timer resumes from the saved play time, and the game over modal remains hidden with full typing and interaction enabled. In Continuous Play, winning or losing the puzzle automatically clears `user://save_continuous.json`, allowing a new random puzzle to begin on subsequent entry. In Daily Challenge mode, in-progress save data is only valid for the current UTC day; if the player enters the Daily Challenge after UTC rollover (detected by comparing the saved `secret_word` with today's expected `daily_word`), any prior day's save state (`user://save_daily.json`) is discarded and cleared rather than deserialized, and a clean game for today's daily word is initialized.

## 8. User Interface & Visual Design
- **REQ-8.1 - 1930s Monochrome UI Aesthetic:** Menus, modals, and UI containers follow a 1930s vintage monochrome animation visual language using high-contrast black, charcoal, and crisp white outlines. Chromatic color is strictly reserved for gameplay deduction evaluation cues (Green `#538d4e` 🟩, Yellow `#b59f3b` 🟨, and Flat Red `#b53b3b` 🟥).
- **REQ-8.2 - Button Styling & Interaction States:** Standard buttons throughout the application must feature:
  - **Normal State:** Dark charcoal fill (`#1c1c1e`), 2px solid white border (`#ffffff`), 8px rounded corners, and crisp white text (`#ffffff`).
  - **Hover/Focused State:** Brightened charcoal fill (`#2c2c30`) with a 2px solid white border.
  - **Pressed State:** Deep black fill (`#0e0e10`) with a 2px solid white border for tactile feedback.
- **REQ-8.3 - Centralized UI Theme System:** Project UI styling must be managed via a centralized Godot theme resource (`res://assets/theme/letter_logic_theme.tres`) configured as the project-wide custom GUI theme. Panel containers feature a deep black fill (`#0e0e10`), 3px solid white border (`#ffffff`), and 12px corner radii, ensuring consistent presentation across all menus, dialogs, and modals.
- **REQ-8.4 - Mascot Character & Application Icon:** A signature 1930s rubber-hose cartoon tile mascot in a dynamic walking pose serves as the official desktop and Android launcher application icon (`res://assets/icons/icon.png`, 512x512 PNG). The mascot features expressive pie-eyes, cartoon white gloves 🧤, a bold vintage letter "L" on its face, thick rounded white ink outlines, and a strictly monochrome palette (Ink Black `#121213` ⬛, Dark Charcoal `#1c1c1e` 🖤, and Crisp White `#ffffff` ⬜) preserving chromatic colors exclusively for deduction feedback. Modern Android exports utilize this mascot artwork across dedicated adaptive foreground and solid background layers (see REQ-8.17).
- **REQ-8.5 - Static Main Menu Mascot:** The signature 1930s walking cartoon mascot (`res://assets/icons/icon.png`, 512x512 PNG) is positioned as the centerpiece on the Main Menu directly above the `LETTERLOGIC` title box (`MascotRect`, centered `pivot_offset` at `Vector2(128, 128)`). The mascot is rendered completely statically with zero rotation (`rotation_degrees = 0.0`), uniform scale (`scale = Vector2.ONE`), and no animation or active tweens.
- **REQ-8.6 - How to Play Modal Specifications:** The How to Play instructions modal (`HowToPlayModal`) displayed from the Main Menu must be enclosed in a responsive `MarginContainer` (24px horizontal, 48px vertical margins) spanning the full viewport, providing an engaging, highly legible reading experience and comfortable touch interaction across standard (720x1280) and varied Android portrait aspect ratios:
  - **Modal Title (`ModalTitle`):** Tripled font size of 84px (`theme_override_font_sizes/font_size = 84`), doubled from 28px, centered prominently at the top of the modal.
  - **Instructional Body Text & Headings (`RulesText`):** Calibrated base body text font size of 32px (`theme_override_font_sizes/normal_font_size = 32`) and bold section headings at 32px (`theme_override_font_sizes/bold_font_size = 32`). The text uses dynamic "shrink-to-fit" font scaling (`RulesTextScaler` in `res://scripts/rules_text_scaler.gd`) that automatically decrements both normal and bold font sizes (down to a minimum safety floor of 14px) when rendered on compact or short portrait screens so all instructional copy fits cleanly within the available vertical space.
  - **Dismiss Button (`CloseButton`):** Enlarged custom minimum height of at least 80px (`custom_minimum_size = Vector2(0, 80)`) and doubled font size of at least 36px (`theme_override_font_sizes/font_size = 36`), providing an ergonomic tap target matching the Main Menu secondary utility buttons.
  - **Inner Content Inset:** The panel content is wrapped in an additional inner `MarginContainer` with 24px left and right margins so the title, instructional body, and dismiss button all remain at least 24px from the panel borders while preserving the outer 48px top and bottom viewport margins.
  - **Container Separation & Disabled Scrolling:** Vertical separation between modal elements is set to 20px (`theme_override_constants/separation = 20`) to provide clean breathing room. The `RulesText` container maintains vertical flex expansion (`size_flags_vertical = 3` / `SIZE_EXPAND_FILL`) with scrolling completely disabled (`scroll_active = false`), relying on the dynamic shrink-to-fit font scaling to eliminate scrolling entirely.
  - **Deduction & Rules Content:** All deduction cues and evaluation color indicators within the modal must strictly reflect the traffic-light color scheme (Green `#538d4e` 🟩, Yellow `#b59f3b` 🟨, and Flat Red `#b53b3b` 🟥), and special rules (5-letter isograms, active row key disabling) and game modes (Daily Challenge UTC midnight reset, Continuous Play unlimited sandbox) must be clearly explained.
- **REQ-8.7 - Responsive Android Portrait Layout & Container Hierarchy:** The user interface across all game screens, menus, and modals must dynamically adapt to diverse Android portrait display dimensions and aspect ratios (e.g., standard 16:9, tall 18:9, 19.5:9, 20:9, 21:9 smartphones, and 4:3, 16:10 portrait tablets) without element clipping, text truncation, or letterboxing distortion:
  - **Display Configuration:** Strict portrait orientation lock (`window/handheld/orientation=1`) and adaptive canvas stretch (`stretch/mode="canvas_items"`, `stretch/aspect="expand"`) with a base reference viewport of 720x1280.
  - **Game Board Grid:** Wrapped in a responsive `MarginContainer` safe-area and an `AspectRatioContainer` (5:6 aspect ratio ~0.8333) utilizing `SIZE_EXPAND_FILL`, ensuring tiles expand dynamically to available width while maintaining square tile proportions.
  - **Letter Tile Typography & Sizing:** Letter tile labels (`Label` inside `Tile`) use an enlarged font size of 72px (`theme_override_font_sizes/font_size = 72` and programmatic fallback `FONT_SIZE_DEFAULT = 72`) centered horizontally and vertically, filling expanded responsive tile boxes (approx. 100px–120px+) proportionally with clean padding from the 2px border and 4px corner radii without clipping or overflow across any supported device aspect ratio.
  - **Virtual Keyboard:** Wrapped in responsive margins with horizontal expansion flags (`SIZE_EXPAND_FILL`), allowing letter keys and weighted control keys (Delete and Enter) to scale cleanly across screen widths. To achieve a mathematically uniform Wordle-style layout without hardcoded pixel widths or auto-sizing discrepancies, the keyboard grid strictly uses Godot's proportional `size_flags_stretch_ratio`:
    - All keys and row spacers configure `size_flags_horizontal = Control.SIZE_EXPAND_FILL`.
    - Standard letter keys are assigned `size_flags_stretch_ratio = 1.0` ensuring mathematically uniform widths across all letter keys (e.g. "W" and "I" have identical widths).
    - Action keys (`ENTER` and `⌫` Backspace) are assigned `size_flags_stretch_ratio = 1.5` (exactly 50% wider than standard letter keys).
    - Row 2 side stagger spacers (`LeftStaggerSpacer` and `RightStaggerSpacer`) are assigned `size_flags_stretch_ratio = 0.5`, centering Row 2 under Row 1 by exactly half a standard key width on each side.
    - Hardcoded X-axis `custom_minimum_size` constraints on keys and spacers are removed (maintaining only the Y-axis touch height requirements, `KEY_MIN_TOUCH_HEIGHT = 44.0`).
    Keys must keep a 44px minimum touch-height floor (`KEY_MIN_TOUCH_HEIGHT = 44.0`) so the keyboard remains fully contained without rigid wrapper offsets or a fixed vertical pixel floor, while safe-area bottom insets reserve breathing room on gesture-navigation devices. The bottom row places the Delete (`⌫`) key on the far left and the Enter (`ENTER`) key on the far right, matching mobile keyboard ergonomics and user muscle memory. All keyboard buttons enforce a 4px horizontal content margin (`content_margin_left = 4`, `content_margin_right = 4`) on their dynamically generated `StyleBoxFlat` backgrounds to hard-enforce a physical visual buffer between text and button edges across narrow aspect ratios. Standard letter keys (20–44px) and the Delete (⌫) icon (20–44px) feature responsive font sizing and margin padding that shrink or expand with the available height without clipping, and the Enter text is scaled to a responsive 22–32px range (`FONT_SIZE_ENTER_MIN = 22`, `FONT_SIZE_ENTER_MAX = 32`), fitting prominently inside the 1.5x-width key boundary without horizontal truncation or border clipping.
  - **Main Game Flow Layout:** The `MainGame` scene uses a single `ContentMargin/VBoxContainer` vertical authority so the `Header`, `ToastOverlay`, `BoardArea`, and `KeyboardArea` remain sequential siblings in one flow. `BoardArea` is an `AspectRatioContainer` that owns the 6x5 board slot, while `KeyboardArea` is a `MarginContainer` that owns only the keyboard's intrinsic minimum height and no negative offset. The keyboard layout flexes so the board and keyboard cannot overlap under any circumstance across any supported mobile viewport.

    Portrait-first proportional layout contract (used by runtime and tests):
    - safe-area-only root margins (ContentMargin keeps device cutouts/gestures out of content flow)
    - side margins: 4% of viewport width, clamped to 12–24 px
    - top breathing buffer: 2% of available height, clamped to 12–36 px
    - header: 11% of available height, clamped to 76–320 px
    - keyboard: 22% of available height, clamped to 160–620 px
    - bottom breathing buffer: 1.75% of available height, clamped to 10–36 px
    - content separation: 0.8% of available height, clamped to 4–12 px
    - board and keyboard participate in proportional expansion with stretch ratio ~2.7 : 1.0 so the board fills the middle while the keyboard retains a readable region

    Note: Continuous Play and Daily Challenge share the same `MainGame` scene and layout contract; any changes to these ranges must be reflected in the tests and manual testing guidance.
  - **Menus & Overlays:** Main Menu, Game Over Modal, How to Play Modal, and Statistics Screen utilize responsive `MarginContainer` padding with expanding flex containers rather than hardcoded pixel dimensions or rigid `CenterContainer` constraints.
- **REQ-8.8 - 1930s Monochrome Tabular Layout & Statistics Screen Typography/Layout Scaling:** Mode switching on the Statistics Screen uses a connected `TabContainer` styled to match the 1930s monochrome aesthetic, with doubled typography and expanded layout components for enhanced mobile legibility:
  - **Opaque Full-Screen Background:** The Statistics modal features a 100% opaque full-screen background (`ColorRect` named `Background`, configured with `color = Color(0.0705882, 0.0705882, 0.0705882, 1)` / `#121212` and full viewport anchors). This completely obscures and hides all underlying screens (including the mascot, title box, and buttons on the Main Menu, or the active timer, letter grid, and keyboard during Continuous Play and Daily Challenge) with zero visual bleed-through. The backdrop retains `mouse_filter = Control.MOUSE_FILTER_STOP` (0) to block touches and clicks from reaching underlying controls.
  - **Modal Header & Dismiss Button:** The modal title (`Title`) font size is doubled to 52px (`theme_override_font_sizes/font_size = 52`). The modal dismiss button (`CloseButton`) font size is doubled to 32px (`theme_override_font_sizes/font_size = 32`) with custom minimum dimensions expanded to `Vector2(60, 60)` for comfortable touch ergonomics.
  - **Mode Tabs Navigation:** Tab title typography is doubled to 32px (`theme_override_font_sizes/font_size = 32`). Selected and unselected tab StyleBoxes (`tab_selected`, `tab_unselected`) configure expanded content margins (20px horizontal, 12px vertical) to cleanly house 32px text without vertical clipping or outline collision.
  - **Active Tab:** Matches content panel background (`#0e0e10`), 3px solid white borders (`#ffffff`) on top, left, and right, with the bottom border removed (`border_width_bottom = 0`) to merge seamlessly into the statistics content panel below. Top corners feature 12px rounded radii, with square 0px bottom corners.
  - **Inactive Tab:** Dark charcoal fill (`#1c1c1e`), 3px solid white borders (`#ffffff`) on all sides (retaining bottom boundary line separating it from the panel), 12px rounded top corners, and square 0px bottom corners.
  - **Content Panel:** Deep black fill (`#0e0e10`), 3px solid white border (`#ffffff`), and 12px corner radii, aligning cleanly with the active tab header without gaps, misalignments, or missing outlines.
  - **Summary Metrics Layout & Typography:** The 6 summary cards are organized in a 3-column `GridContainer` (2 rows of 3 columns, with 8px horizontal and 16px vertical separation) ordered to establish intuitive vertical column pairings:
    - **Top Row (Overall Milestones & Peak Achievements):** `PlayedCard` (Row 1, Column 1), `MaxStreakCard` (Row 1, Column 2), and `BestTimeCard` (Row 1, Column 3).
    - **Bottom Row (Rates, Active Status & Averages):** `WinPctCard` (Row 2, Column 1), `StreakCard` (Row 2, Column 2), and `AvgTimeCard` (Row 2, Column 3).
    - **Column Pairings:** Column 1 pairs volume with win rate (Played / Win %), Column 2 pairs peak with current streak (Max Streak / Current Streak), and Column 3 pairs fastest with average solve duration (Best Time / Avg Time).
    - **Typography:** Metric values are enlarged to 36px font size (`theme_override_font_sizes/font_size = 36`), and metric category labels are enlarged to 18px font size (`theme_override_font_sizes/font_size = 18`).
  - **Guess Distribution Graph & Bar Scaling:** The distribution heading (`DistHeading`) is enlarged to 24px (`theme_override_font_sizes/font_size = 24`) with 8px separation in `DistributionContainer`. Each distribution row (`HBoxContainer`) custom minimum height is increased from 28px to 48px (`custom_minimum_size = Vector2(0, 48)`). Row indicator labels (`1`..`6`, `X`) are enlarged to 24px font size with `Vector2(36, 0)` minimum width. Distribution bar count labels (`count_lbl`) are enlarged to 22px font size with right alignment inside `bar_panel`. Bar panels feature 6px rounded corners (`corner_radius = 6`) and scale proportionally with min width stretch factor (0.08) and companion right spacer.
- **REQ-8.9 - Main Game Vertical Hierarchy, Center Header Typography & Persistent Toast Overlay Space:** The primary gameplay screen (`main_game.tscn`) organizes its visual layout strictly inside a responsive `ContentMargin -> VBoxContainer` flow with mathematically constrained top-to-bottom hierarchy. `ContentMargin` applies both top and bottom safe-area insets on gameplay screens, `BoardArea` is an `AspectRatioContainer` that owns the 6x5 board slot, and `KeyboardArea` is a `MarginContainer` that owns only the keyboard minimum size:
  1. Title (`LETTERLOGIC`)
  2. Game Mode Label (`DAILY CHALLENGE • YYYY-MM-DD` / `CONTINUOUS PLAY`)
  3. Active Puzzle Timer (`00:00`)
  4. Toast Popup Notification (`ToastOverlay` / `ToastPanel` / `MarginContainer` / `ToastLabel`)
  5. Game Board Grid (`BoardArea` / `GameBoard`)
  6. On-Screen Virtual Keyboard (`KeyboardArea` / `Keyboard`)

  **Center Header Typography Scaling:**
  To optimize readability and scanning speed on mobile screens (720x1280 base reference and tall Android portrait aspect ratios), center header text elements feature doubled font sizes:
  - **Game Title (`TitleLabel`):** 48px font size (`theme_override_font_sizes/font_size = 48`), establishing a bold brand presence balanced with the enlarged letter board tiles.
  - **Mode & Date Subtitle (`ModeLabel`):** 28px font size (`theme_override_font_sizes/font_size = 28`), ensuring high legibility for active mode and UTC date context.
  - **Active Timer (`TimerLabel`):** 32px font size (`theme_override_font_sizes/font_size = 32`), allowing solve duration to be scanned at a glance.

  **Toast Popup Typography & Padding:**
  - **Toast Notification (`ToastLabel`):** 36px font size (`theme_override_font_sizes/font_size = 36`), providing prominent alert visibility.
  - **Toast Horizontal Padding:** The toast outline container (`ToastPanel`) wraps the label in a `MarginContainer` configured with 24px horizontal margins (`margin_left = 24`, `margin_right = 24`) and 8px vertical margins (`margin_top = 8`, `margin_bottom = 8`), guaranteeing comfortable breathing room away from the 12px rounded outline corners and preventing letter crowding.

  **Persistent Toast Space & Layout Reservation:**
  The `ToastOverlay` is integrated natively into the `VBoxContainer` directly between `Header` and `BoardArea` without hardcoded pixel offsets. To prevent UI jitter or vertical shifting of the letter grid (`GameBoard`), vertical space for `ToastOverlay` is persistently reserved in the layout hierarchy (maintaining `visible = true`). When inactive, it is rendered fully transparent (`modulate.a = 0.0`). When activated, it smoothly transitions in via alpha modulation, rendering horizontally centered within the reserved vertical gap between the header timer and letter grid without shifting, obscuring, clipping, or overlapping adjacent UI elements across any screen dimensions or aspect ratios.
- **REQ-8.10 - Header Statistics Button & Monochrome Icon:** The statistics button (`StatsButton`) in the top-right corner of the main game header uses a custom monochrome bar graph icon texture (`res://assets/icons/stats_icon.png`) with an empty text string, replacing the default emoji (`📊`). The icon depicts four solid white vertical bars and a horizontal baseline underline in 100% solid white (`#ffffff` / `Color(1, 1, 1, 1)`) on a transparent background (`rgba(0, 0, 0, 0)`), completely free of black ink outlines or double-outline artifacts to maximize legibility and visual contrast against the dark charcoal button background (`#1c1c1e` normal / `#2c2c30` hover). The icon is centered (`icon_alignment = 1`) and expanded (`expand_icon = true`), scaling cleanly without distortion. To provide optimal touch ergonomics and padding on mobile displays, `StatsButton` and `BackButton` both feature an enlarged minimum size of `Vector2(56, 56)` within a 56px height header (`VBoxContainer/Header`), preserving visual symmetry and alignment across the top navigation bar.
- **REQ-8.11 - Header Navigation & Back Button Arrowhead:** The navigation back button (`BackButton`) in the top-left corner of the main game header uses a bold, clean left-pointing arrowhead character (`<`) with an enlarged font size of 32px (`theme_override_font_sizes/font_size = 32`) and crisp solid white font color (`theme_override_colors/font_color = Color(1, 1, 1, 1)`), replacing the thin legacy arrow glyph (`←` / `U+2190`). This eliminates the thin tail line and indistinct arrow symbol, filling the button area prominently with balanced margins and high visual contrast against the dark charcoal button background (`#1c1c1e` normal / `#2c2c30` hover). The button maintains `custom_minimum_size = Vector2(56, 56)` to ensure ergonomic touch interaction and visual symmetry with the companion `StatsButton` across the 56px header, returning players smoothly to the Main Menu (`res://scenes/main_menu.tscn`) from both Daily Challenge and Continuous Play modes without state leaks or unhandled input.
- **REQ-8.12 - Main Menu Typography & Navigation Button Sizing:** To deliver an engaging, legible, and ergonomically balanced mobile experience on standard (720x1280) and varied Android portrait aspect ratios, the Main Menu interface features doubled typography specifications and expanded button dimensions:
  - **Game Title (`TitleLabel`):** 88px font size (`theme_override_font_sizes/font_size = 88`), doubled from 44px, establishing an iconic and prominent brand identity above the navigation options.
  - **Subtitle Tagline (`SubtitleLabel`):** 36px font size (`theme_override_font_sizes/font_size = 36`), doubled from 18px, rendered in `#b3b3b3` (`Color(0.7, 0.7, 0.7, 1)`).
  - **Title Container Separation:** `TitleBox` separation set to 12px (`theme_override_constants/separation = 12`) to structure the title and subtitle cleanly without vertical overlap or crowding.
  - **Primary Mode Buttons (`DailyButton` & `ContinuousButton`):** Font size doubled to 40px (`theme_override_font_sizes/font_size = 40`) and custom minimum height increased from 68px to 120px (`custom_minimum_size = Vector2(0, 120)`), comfortably fitting two lines of text (including active countdowns like `Daily Challenge\n[Next in: 04:15:22]` and `Continuous Play\n[Unlimited Practice]`) with generous padding and no text truncation or vertical clipping against the 2px white border and 8px corner radii.
  - **Secondary Utility Buttons (`StatsButton`, `HowToPlayButton` & `CreditsButton`):** Font size doubled to 36px (`theme_override_font_sizes/font_size = 36`) and custom minimum height increased from 56px to 80px (`custom_minimum_size = Vector2(0, 80)`), providing comfortable padding and generous mobile touch targets. The How to Play instructions modal (`HowToPlayModal`) and Credits modal (`CreditsModal`) reflect matching typography scaling and touch ergonomics with 84px and 64px titles respectively, legible body/attributions, and enlarged 80px height dismiss buttons (`CloseButton`) with 36px font.
  - **Menu Buttons Separation:** `MenuButtons` vertical separation set to 20px (`theme_override_constants/separation = 20`) to cleanly separate options without visual crowding.
  - **Vertical Budget & Centering:** The vertical layout hierarchy (`VBox`) remains centered within the responsive `MarginContainer` safe area, occupying ~990px of the 1184px vertical budget with ~194px of vertical breathing room across standard and tall Android portrait aspect ratios.
- **REQ-8.13 - Custom Boot Splash Screen:** The application startup boot splash screen is configured in `game/project.godot` to display the signature 1930s walking cartoon tile mascot icon (`res://assets/icons/icon.png`) with image filtering enabled (`boot_splash/use_filter = true`) and image visibility enabled (`boot_splash/show_image = true`). The boot splash background color is set to `Color(0.0705882, 0.0705882, 0.0705882, 1)` (`#121212`), matching the project's default clear color and monochrome background palette for a seamless, flicker-free visual transition directly into the Main Menu (`res://scenes/main_menu.tscn`). The boot splash screen strictly omits all text, titles, subtitles, version numbers, or taglines, exclusively displaying the mascot icon graphic.
- **REQ-8.14 - Main Menu Credits Button, Attributions Modal & Studio Links:** To provide clear attribution to project creators, publishers, and the open-source community, the Main Menu includes an interactive Credits screen accessible via a dedicated utility button:
  - **Credits Button (`CreditsButton`):** Positioned in `MarginContainer/VBox/MenuButtons` directly below `HowToPlayButton`. Configured with `text = "Credits"`, doubled 36px font size (`theme_override_font_sizes/font_size = 36`), and custom minimum height of 80px (`custom_minimum_size = Vector2(0, 80)`), matching the touch target ergonomics and styling of `StatsButton` and `HowToPlayButton`.
  - **Credits Modal Overlay (`CreditsModal`):** Implemented as a responsive full-viewport overlay with a dark dimmed background (`Color(0, 0, 0, 0.8)`), 1930s themed `PanelContainer` with 3px white border, 12px rounded corners, and an expanded `MarginContainer` safe area with 64px horizontal and 96px vertical margins (`margin_left = 64`, `margin_right = 64`, `margin_top = 96`, `margin_bottom = 96`) providing comfortable padding between the modal window and screen edges.
  - **Modal Header (`ModalTitle`):** Displays centered title `CREDITS` calibrated to 64px typography (`theme_override_font_sizes/font_size = 64`) to balance visual hierarchy with compact mobile vertical budgets.
  - **Non-Scrolling Mobile Viewport Budget & Safeguard Scroll:** Sizing across all modal elements (title, rows, padding, dismiss button) is calibrated so all content fits cleanly within a standard 720x1280 mobile portrait viewport without requiring scrolling. A `ScrollContainer` (`size_flags_vertical = 3`) wraps the attribution entries (`VBoxContainer` with expanded 48px row separation) as a safeguard for smaller or split-screen displays.
  - **Horizontal Row Attribution Layout:** Replaces vertical stacks with compact horizontal row containers (`HBoxContainer`, 16px internal separation) for each attribution block:
    - **Left Column (Logo Badge):** Uniform circular badge logo graphic (`TextureRect`, custom minimum size `Vector2(144, 144)`, increased 50% from 96x96 for visual prominence, `expand_mode = 1`, `stretch_mode = 5`) centered vertically on the left.
    - **Right Column (Text & Link Container):** `VBoxContainer` with `size_flags_horizontal = 3` (Expand/Fill) and center alignment containing:
      - **Attribution Label:** Calibrated 24px font size (`theme_override_font_sizes/font_size = 24`) with left alignment and word wrapping (`autowrap_mode = 3`).
      - **Web Icon Link:** Clickable 1930s monochrome web/globe icon button (`WebIconBtn`, custom minimum size `Vector2(48, 48)`, `size_flags_horizontal = 0` / `SIZE_SHRINK_BEGIN`, `expand_icon = true`) left-justified directly below the attribution label that dispatches the target destination URL via `OS.shell_open()`.
    - **No Visible URL Text:** Long URL strings (`https://...`) are completely removed from label and button text to eliminate clutter.
  - **Studio & Repository Attributions:**
    1. **Open Game Stack:** High-resolution circular badge logo (`res://assets/icons/OpenGameStackMonoChrome.png`), attribution text `"Developed by Open Game Stack"`, and interactive web icon button launching `https://opengamestack.org/`.
    2. **Audrain Entertainment:** Circular badge logo (`res://assets/icons/AudrainEntertainment.png`), attribution text `"Published by Audrain Entertainment"`, and interactive web icon button launching `https://audrain.games/`.
    3. **GitHub Open Source:** High-resolution circular badge logo (`res://assets/icons/github_icon.png`), attribution text `"LetterLogic is an open-source game hosted on GitHub"`, and interactive web icon button launching `https://github.com/OpenGameStack-Games/LetterLogic`.
  - **Modal Dismissal (`CloseButton`):** Includes a prominent dismiss button at the bottom of the modal with `custom_minimum_size = Vector2(0, 80)`, font size 36px (`theme_override_font_sizes/font_size = 36`), and text `"Got It!"`. Tapping the dismiss button hides `CreditsModal`.
- **REQ-8.15 - Staggered Tile Reveal Animation & Scale Pop:** When a valid guess is submitted, tile color and state evaluations are revealed sequentially from left to right across the active row rather than instantly. Tile reveals are staggered with an incremental delay of 0.3 seconds per column (`col * 0.3`). As each tile's evaluation is revealed, it plays a scale pop animation (scaling up to 1.1x with `TRANS_SINE` / `EASE_OUT` over 0.15s, then returning to 1.0x with `TRANS_SINE` / `EASE_IN` over 0.15s) centered around its midpoint pivot (`pivot_offset = size / 2.0`). This creates a tactile, dynamic reveal effect while keeping the parent `GridContainer` and `AspectRatioContainer` layout completely stable without recalculation jitter or layout shift. The animation applies uniformly across Continuous Play and Daily Challenge modes.
- **REQ-8.16 - Game Over Modal Reveal Delay & Entrance Animation:** Upon puzzle completion (win or loss), presentation of `GameOverModal` is delayed by 1.5 seconds to allow the full staggered tile reveal animation across all 5 columns to finish. When the modal becomes visible, it performs a coordinated entrance animation: starting with 0.0 opacity and 0.8x scale centered at its midpoint pivot (`pivot_offset = size / 2.0`), it fades in to 1.0 opacity over 0.3 seconds while popping to 1.0x scale using `TRANS_BACK` and `EASE_OUT` over 0.3 seconds. This animation executes uniformly across win and loss conditions in both Continuous Play and Daily Challenge modes.
- **REQ-8.17 - Android Adaptive Launcher Icons:** To ensure native display and proper icon masking across modern Android devices (API 26+), the Android export preset (`game/export_presets.cfg`) configures Android Adaptive Icons using separate foreground and background layers at 432x432 resolution. The adaptive foreground layer (`res://assets/icons/icon_foreground.png`, 432x432 PNG) renders the 1930s cartoon tile mascot centered on a transparent background, while the adaptive background layer (`res://assets/icons/icon_background.png`, 432x432 PNG) provides a solid dark background (`#121213`). This configuration enables modern Android launchers to apply system-defined adaptive shapes (circular, squircle, rounded square) and parallax motion without clipping the mascot artwork or displaying artificial borders.
- **REQ-8.18 - Android Safe Area / Display Cutout Insets & Non-Immersive System UI:** Top-level full-screen gameplay and menu layouts must respect Android display cutouts by reading `DisplayServer.get_display_safe_area()` at runtime and adding its insets to the screen's base design margins rather than using hardcoded offsets. To behave like a standard utility/puzzle application and guarantee accurate hardware cutout and safe area detection by the operating system, Android Immersive Mode is explicitly disabled (`screen/immersive_mode=false` in `game/export_presets.cfg`), ensuring that the system status bar (clock, battery) and navigation bar remain visible at all times. The Main Game scene keeps a 70px base top breathing room in `ContentMargin` and adds any non-zero safe-area inset to that margin plus any non-zero gesture/navigation inset to `ContentMargin` bottom margin; the Main Menu and Statistics Screen keep their original 48px top spacing while adding any non-zero safe-area inset to their root `MarginContainer` top margin. All screens adapt smoothly to the reduced vertical viewport height without element collision. The safe-area adjustment must apply on startup and again on resize so punch-hole, notch, and gesture-navigation devices keep the relevant UI fully visible without adding extra padding on devices whose safe-area inset is 0.

## 9. Build, CI/CD & Platform Requirements
- **REQ-9.1 - Android 16 KB Memory Page Size Compatibility (ELF PT_LOAD & ZIP data alignment):** To satisfy Google Play Console submission requirements for Android 15+ (API 35+) and support devices with 16 KB memory page sizes, the Android export process must ensure two complementary properties:
  1. The 64-bit native shared libraries bundled with the app (arm64-v8a and x86_64) must be built with ELF PT_LOAD program-segment alignment of 16 KiB (16384 bytes). This property is determined by the native libraries inside the Godot Android templates; AGP/Gradle packaging cannot rewrite ELF load-segment alignment of prebuilt binaries.
  2. APKs generated from the App Bundle (`.aab`) must store native libraries on 16 KiB data boundaries (ZIP data alignment). Release CI must verify this by building APKs from the signed AAB using Bundletool and running ZIP alignment checks (for example: `bundletool build-apks --bundle=... --output=... --mode=universal` followed by `zipalign -c -P 16 -v 4` on the generated APKs).

  Ensure the project uses Godot Android templates whose 64-bit template libraries expose PT_LOAD alignment of 16 KiB (for example, Godot 4.7.2 templates). Keep the Android Gradle Plugin and Gradle wrapper in sync with the chosen Godot template support (the PR updates use AGP `8.6.1` and Gradle `8.11.1` for Godot `4.7.2`).

  The release CI pipeline must run both an ELF/PT_LOAD validation and a strict ZIP validation (scripts/validate_android_16kb.py), and fail the release if either check reports misaligned or compressed native libraries.
- **REQ-9.2 - Android Native Debug Symbols & Obfuscation Mapping Export:** To enable crash symbolication and ANR debugging in the Google Play Console, the Godot Android export preset (`game/export_presets.cfg`) must enable native debug symbol generation (`gradle_build/export_debug_symbols=true`). Furthermore, the release CI/CD workflow (`.github/workflows/android_release.yml`) must collect the generated native debug symbols archive (`*-native-debug-symbols.zip`) and ProGuard/R8 obfuscation mapping file (`mapping.txt`) alongside the Android App Bundle (`.aab`) and publish them as release artifacts.
- **REQ-9.3 - Android Runtime Dictionary Packaging & Fail-Loud Startup:** The Android export preset (`game/export_presets.cfg`) must explicitly package the runtime dictionary asset(s) used by `WordBank`, including plain-text files such as `res://assets/words/words.txt`, via an export `include_filter` (for example, `*.txt`) whenever `export_filter="all_resources"` is used. At runtime, `WordBank.load_words()` must continue to treat a missing or empty dictionary as a startup error by returning `false` and emitting a clear `push_error(...)` message instead of silently accepting an empty word bank.
- **REQ-9.4 - Android Native Share Sheet Plugin Integration:** The Android export preset (`game/export_presets.cfg`) and project editor plugins (`game/project.godot`) configure and enable the Godot Android `SharePlugin` (`plugins/SharePlugin=true`, `plugins/GMPShared=true`) with precompiled debug and release AAR libraries (`SharePlugin-debug.aar`, `SharePlugin-release.aar`) located under `res://addons/SharePlugin/`. At runtime on Android, `ShareManager` dynamically loads and instantiates the `Share.gd` wrapper node (`res://addons/SharePlugin/Share.gd`), adds it to the scene tree, and invokes `share_text()` to open the native system share sheet via the underlying `SharePlugin` singleton. It cleans up the temporary node with a short timer delay, safely falling back to clipboard copy if the wrapper cannot be loaded or when running on non-Android platforms.
